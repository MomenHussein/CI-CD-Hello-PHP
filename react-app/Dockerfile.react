# Dockerfile
FROM node:18-alpine

WORKDIR /react-app

COPY react-app/package.json ./
RUN npm install

COPY react-app ./

RUN npm run build

EXPOSE 3000
CMD ["npx", "serve", "-s", "build", "-l", "3000"]
